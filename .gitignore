# python
__pycache__/
*.pyc

# macOS
.DS_Store

# Note: docs/ (the generated site) IS committed — GitHub Pages serves it.

# local file-based snapshots (own history; not tracked in git)
versions/

# Claude Code local dev config (machine-specific absolute paths)
.claude/
